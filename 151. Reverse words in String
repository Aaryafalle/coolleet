class Solution {
    public String reverseWords(String s) {
        s= s.trim();
        String[] allwords = s.split("\\s+");
        String ans= "";
        for(int i=allwords.length-1;i>=0;i--){
            ans=ans+allwords[i]+" ";
        } return ans.trim();
    }
   
}
